# TODO

## Before WC Start

- Verify and add per-match API-FOOTBALL fixture IDs for all group-stage matches.
- Run a live dry-run of `python -m tippnation.results_cli --dry-run --force`.
- Decide where the 5-minute result poller runs and monitor its first match day.

## Knockout Stage

- Replace generated knockout placeholders with FIFA-confirmed venues, kickoff times, and pairings once qualifiers are known.
- Add provider fixture IDs for knockout matches after API-FOOTBALL/Betfair expose stable mappings.
