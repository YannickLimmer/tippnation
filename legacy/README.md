# Legacy CSV/Sheets App

These files are the old Streamlit implementation that used CSV/Google Sheets style
state. The active app entrypoint is `../app.py`, and new persistence/scoring work
should live under `../tippnation/`.
